import Link from "next/link";import {notFound} from "next/navigation";import {projects,getProject} from "../../../lib/projects";export const dynamic="force-static";export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}export default async function Page({params}){const {slug}=await params;const p=getProject(slug);if(!p)notFound();return <main><section className="wrap project-head"><Link className="back" href="/">← Back to all projects</Link><div className="kicker">Project</div><h1>{p.title}</h1><p>{p.description}</p></section><section className="wrap section">{p.youtube.length>0&&<><h2>Videos</h2><div className="media-grid">{p.youtube.map((id,i)=><div className="media" key={id}><iframe src={`https://www.youtube.com/embed/${id}?rel=0`} title={`${p.title} video ${i+1}`} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen/><div className="label">Video {String(i+1).padStart(2,"0")}</div></div>)}</div></>}{p.instagram.length>0&&<><h2 style={{marginTop:60}}>Instagram</h2><div className="media-grid">{p.instagram.map((id,i)=><div className="media instagram" key={id}><iframe src={`https://www.instagram.com/p/${id}/embed`} title={`${p.title} Instagram ${i+1}`} scrolling="no" allowTransparency allowFullScreen/><div className="label">Instagram {String(i+1).padStart(2,"0")}</div></div>)}</div></>}</section></main>}